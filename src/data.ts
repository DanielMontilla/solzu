/**
 * @internal
 * @description property key for custom object's unique symbol classifier
 */
export const $CLASSIFIER = "classifier" as const;

/**
 * @internal
 * @description property key for custom object discriminator field
 */
export const $SPECIFIER = "kind" as const;
